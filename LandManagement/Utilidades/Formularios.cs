using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Reflection;
using System.Windows.Forms;

namespace LandManagement.Utilidades
{
    public class Formularios
    {
        private void ChildClickOld(object sender, EventArgs e)
        {
            //Datos datos = new Datos();
            //var sbm = datos.obtenerSubmenus().Where(x => x.frm_name == sender.ToString()).FirstOrDefault();
            //string frmCode = ((submenu)sbm).frm_code;

            //Assembly frmAssembly = Assembly.LoadFile(Application.ExecutablePath);

            //foreach (Type type in frmAssembly.GetTypes())
            //{
            //    if (type.BaseType == typeof(Form))
            //    {
            //        if (type.Name == frmCode)
            //        {
            //            Form formShow = (Form)frmAssembly.CreateInstance(type.ToString());

            //            foreach (Form form in this.MdiChildren)
            //                form.Close();

            //            formShow.MdiParent = this;
            //            formShow.WindowState = FormWindowState.Maximized;
            //            formShow.Show();
            //        }
            //    }
            //}

        }
    }
}
